installs and configures a react development environment with the latest dependencies

- run `npm i` to install dependencies
- run `npm start` to build and run the project
- go to localhost:3000 on your browser
